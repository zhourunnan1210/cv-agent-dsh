# cv-agent-dsh 的 bundle 层（宿主平面）。
#
# 服务平面归属已裁定（勘误 §4.4.1，2026-09-15）：
#
# - preset 平面的行（projectState / kb / ideaScore 及其工具行）不在这里——
#   它们由 cv-research preset 的组合文件挂载，且必须包在带 `isolate` realm 的
#   group 内（见 docs 勘误 §4.4）。
# - 本文件只承载**宿主平面**的行。当前有一行：MinerU 额度账本
#   （2000 页/天是跨项目共享资源，docs/mineru-api.md §8.3；expOrchestrator 的
#   跨项目资源账本——GPU 实例、预算配额——后续按同规则入驻）。
#
# `dsh plugin --profile <name> add` 会把本包追加进 profile 的 bundle 层栈。
- insert:
    - id: mineru-quota
      name: cv-agent-dsh/mineru-quota
