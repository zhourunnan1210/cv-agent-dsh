# 密钥模板：复制为 .env.local 后填入真实值。
#
#   Copy-Item .env.example .env.local
#
# `.env.local` 已被 .gitignore 的 `.env.*` 规则排除；**本文件（.env.example）
# 是唯一入库的**，只放变量名与说明，永远不要写真实密钥（NOTICE.md 红线 #4）。
#
# 读取方式：scripts/start-dsh-web.ps1 会加载 .env.local 并导出为环境变量。
# 已经存在于环境里的同名变量优先，不会被覆盖。

# Asta MCP 的 x-api-key（Ai2 Scientific Corpus Tool）。
# 供 cv-research preset 的 mcp-asta 行使用；缺失时该 Agent 会没有检索工具。
ASTA_API_KEY=

# MinerU 官方 API token（PDF → Markdown 解析）。
# 仅在调用 MinerU 解析服务时需要，宿主启动不依赖它。
MINERU_TOKEN=

# Unpaywall 要求带 email 的查询（paper-fetch skill 的第 1 个解析源会用到）。
UNPAYWALL_EMAIL=
