# 重启后的待办交接（2026-09-18 夜）

> 背景：本会话发现**新提取协议未生效**（宿主进程跑的是旧代码），用户裁定"先重启，再提取"。
> 本文件把重启后要做的第一件事写死，避免新会话从零摸索。

---

## 一、重启后第一件事：提取 120 篇（用户已裁定范围）

### 一句话

**只提取 `data/papers/extraction-list-relevant.txt` 里那 120 篇**（已解析但未提取、且与课题相关）。
**不要**提取音频方向（56 篇）与其他方向（22 篇）——用户 2026-09-18 明确排除。

### 清单

| 文件 | 内容 |
|---|---|
| `data/papers/extraction-list-relevant.txt` | 120 个 `paper_id`，每行一个 |
| `data/papers/extraction-list-relevant.json` | 同上 + 标题 + 分组 |

**分组构成**（这是"相关"的口径，写在这里免得被质疑）：

| 组 | 篇数 | 说明 |
|---|---|---|
| 人脸/图像深伪 + 持续学习 | **1** | 正中课题：`10.1109/iccv51070.2023.00738`（Rehearsal-Free Domain Continual Face Anti-Spoofing: Generalize More and Forget Less, ICCV 2023） |
| 人脸/图像深伪检测 | 119 | 课题的"要保住的泛化能力"证据来源 |
| 持续学习方法学 | 0 | 标题命中为 0（该类存量此前已提取） |
| **[排除] 音频深伪** | 56 | 用户明确"不做音频" |
| **[排除] 其他** | 22 | 需人工判方向 |

### 执行方式

逐篇调 `cvagent_kb_extract`。**建议先做那 1 篇正中课题的**，验证新契约生效后再铺开。

---

## 二、必须先验证新协议真的生效了

### 为什么

本会话实测：构建产物里**已有**新契约，但宿主进程启动于 **09-17 22:00**，而产物写入于
**09-18 22:23**——宿主跑的是旧代码，递归出来的 Reader 用旧契约，产出：

```
method_modules: 0 个            ← 新协议核心字段，空
problem_statement: 601 字       ← 契约要求 150–300
method_summary:   1740 字       ← 契约要求 500–800
```

### 重启后第一件事：验证

提取任意一篇，然后用下面这条命令核对（可复跑）：

```bash
node data/papers/_verify-extraction-contract.mjs
```

**通过判据**（三条都要）：
1. `method_modules` **3–6 个**，每个含 `name` / `role` / `description`(60–400 字) / `kind`
2. `problem_statement` 150–300 字、`method_summary` 500–800 字
3. `benchmarks` 只含数据集/基准名（不含生成器名或实验设定描述）

不通过 → 新代码仍未加载，别继续铺开。

### 新协议出处（设计与实现）

- 设计：`docs/论文库与撞车打分整合设计-v1.0.md` §3.2（L2 提取粒度契约）、§9 第 3 步
- 契约唯一权威：`packages/core/src/granularity.ts`（`GRANULARITY`，Reader 与 Generator 共用）
- 实现：`packages/dsh-plugin/src/kb/extract-tool.ts`（产出后自动汇入模块清单 L3.6）

---

## 三、本会话已完成的其他工作（不要重做）

| 工作 | 状态 | 位置 |
|---|---|---|
| 论文抓取链路修复（编码崩溃 + arXiv 406） | ✅ 已修已验 | `docs/E34-paper-fetch-抓取链路修复记录.md` |
| 本地 MinerU 打通并跑完一批 | ✅ 66/66 成功，零额度 | `docs/E34-本地MinerU打通记录.md` |
| 本地解析产出整合入库 | ✅ 已按仓库约定规整 | `parse_channel='mineru-local'` |
| 质量关 | ✅ typecheck 四包过；test 410 全过 | — |
| 旧格式污染记录清理 | ✅ 已删那条 0 模块的提取 | — |

**当前库状态**：论文 424 / 已解析 250 / 已提取 52 / 待提取 198（其中相关 120）。

---

## 四、重启前先想清楚的一件事

那 56 篇音频论文与 22 篇其它方向论文，**要不要从库里清掉**是个独立决定（用户未裁定）。
它们已经解析（占磁盘、也会进检索召回），但不在课题范围内。**不要擅自删**——
清理语料是破坏性操作，需用户明确指示。
