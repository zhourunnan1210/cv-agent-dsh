packages:
  - 'packages/*'
  - 'packages/vendor/*'
allowBuilds:
  # onnxruntime-node 的原生二进制靠 postinstall 下载/链接；pnpm 默认拦截构建脚本，
  # 不显式允许的话 transformers 一加载就报"找不到运行时"。
  onnxruntime-node: true
  # onnxruntime-node 的传递依赖，同样需要构建。
  protobufjs: true
